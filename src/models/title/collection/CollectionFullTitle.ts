import {CollectionFullCollectionTitle} from "./CollectionFullCollectionTitle";

export class CollectionFullTitle {
    full: CollectionFullCollectionTitle;

    constructor() {
        this.full = new CollectionFullCollectionTitle();
    }
}